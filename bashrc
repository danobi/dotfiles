#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '


alias sch='cd /home/daniel/Documents/School'
alias sco='xset dpms force off'
alias lk='xscreensaver-command --lock'
alias c='clear'
